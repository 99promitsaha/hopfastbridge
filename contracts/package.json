{
  "name": "hopfast-architect-escrow",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/compile.mjs",
    "test": "npm run build && npm --prefix ../backend run build && node --test test/*.test.mjs",
    "deploy": "node --env-file-if-exists=.env scripts/deploy.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.4.0",
    "ethers": "6.15.0"
  },
  "devDependencies": {
    "solc": "0.8.30",
    "ganache": "7.9.2"
  }
}
